# Troubleshooting FAQ

Validated against commit/tag: `<fill-me>`

Canonical source of failure signatures and fixes:

- `MsMoEMaker/docs/TROUBLESHOOTING.md`

## Quick index

- Build fails with missing module -> install `[train]`
- Resume refused due to mismatched build -> use `--force` or original defaults
- Smoke timeout or llama.cpp path issues -> verify runtime path and timeout
- Command mismatch/errors -> verify installed command set via `describe`

For exact signatures and current remediation steps, always defer to canonical repo troubleshooting docs.
